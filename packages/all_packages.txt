requests==2.32.3
colorama==0.4.6
re==built-in module (version depends on the Python version)
sys==built-in module (version depends on the Python version)
time==built-in module (version depends on the Python version)
threading==built-in module (version depends on the Python version)
os==built-in module (version depends on the Python version)
